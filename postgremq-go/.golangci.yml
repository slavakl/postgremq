version: "2"

run:
  timeout: 5m
  tests: true

linters:
  # Enable default linters
  enable-all: false
  disable:
    - errcheck  # Too noisy in tests

issues:
  # Maximum issues count per one linter
  max-issues-per-linter: 0
  # Maximum count of issues with the same text
  max-same-issues: 0
